name: arruma_ufpr_app
description: App ArrumaUFPR

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.6.5
  mask_text_input_formatter: ^2.4.0
  dio: ^5.0.0
  flutter_svg: ^1.1.6
  get_storage: ^2.0.3
  image_picker: ^0.8.6+3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - "assets/images/"
    - "assets/icons/"